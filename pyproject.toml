[project]
name = "practical-etl-demos"
version = "0.1.0"
description = "Minimal ETL demos for Airflow, Prefect, and Dagster"
requires-python = ">=3.10"
dependencies = [
    "prefect",
    "dagster",
    "dagster-webserver",
    "kafka-python",
    "psycopg2-binary",
    "sqlalchemy",
    "boto3",
]

[tool.uv]
package = false
